{% set rawSlug = page.url | replace("/", "") %}
{% set emailSlug = rawSlug if rawSlug else "home" %}
{% set contactEmail = "digitalgarden+" + emailSlug + "@alexbaron.me" %}
<section class="contact-prompt">
  <p>
    Anmerkungen oder Fragen?
    <a href="mailto:{{ contactEmail }}">Schick mir eine Mail.</a>
  </p>
</section>
